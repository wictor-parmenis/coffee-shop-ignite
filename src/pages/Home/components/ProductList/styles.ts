import styled from 'styled-components';

export const ProductListContainer = styled.div`
    margin-top: 4rem;
    padding-bottom: 4rem;
`;

export const ProductListMain = styled.div`
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
`;

export const TitleProductList = styled.h3`
    font-size: 2.2rem;
    font-weight: bold;
    font-family: 'Baloo 2', cursive;

    margin-bottom: 2rem;
`;
